#include <stdio.h>

int main() {
    // Write C code here
    int num;
    scanf("%d",&num);
        for(int i=0;i<num;i++)
            {
                if(i%2==0)
                {
                printf("%d\n",i);
            }
            }
    return 0;
}
